[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "ferrocrypto"
version = "0.1.0"
description = "Rust crypto core for ferrovault (Argon2id + XChaCha20-Poly1305)."
requires-python = ">=3.10"

[tool.maturin]
module-name = "ferrocrypto"
